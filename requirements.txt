# Runtime deps cho macOS dev — pinned cụ thể cho reproducibility
# Cài: pip install -r requirements.txt
# Khuyến nghị: pip install -e ".[dev]" (dùng pyproject.toml)

PyQt6==6.7.1
mediapipe==0.10.18
opencv-python==4.10.0.84
numpy==1.26.4
Pillow==10.4.0
qrcode[pil]==7.4.2
loguru==0.7.2
requests==2.32.3
